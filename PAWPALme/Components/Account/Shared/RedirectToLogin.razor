@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/auth?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", true);
    }
}
